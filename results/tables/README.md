# Result tables

Each table is regenerated from saved fold-level predictions. When you run the pipeline, a
`<table>.metadata.md` is expected alongside each CSV recording: generating script, config,
manuscript table/figure, run date, seed (20260529), and input-data version. Real result CSVs
are not committed (they contain real phenotypes); see `docs/data_availability.md`.
